//Find 2nd highest value in array

arr.sort()
arr.reverse()
for x in range(1,len(arr)):
    if arr[0] != arr[x]:
        print(arr[x])
        break


// 2nd option b y removing duplicates

# Python program to find 2nd largest number
# in a list
 
# List of numbers
list1 = [10, 20, 20, 4, 45, 45, 45, 99, 99]
 
# Removing duplicates from the list
list2 = list(set(list1))
 
# Sorting the  list
list2.sort()
 
# Printing the second last element
print("Second largest element is:", list2[-2])
